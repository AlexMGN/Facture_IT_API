const { validateRequest } = require('../../utils/validators');

module.exports = (router, check, restrictedRoutesMethods) => {

    /* Users */

    /* Clients */

    /* Factures */

    /* Devis */

    /* Outils comptable */

    /* Statistiques */

    return router
}